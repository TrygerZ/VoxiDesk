customtkinter>=5.2.0
faster-whisper>=1.0.0

# ⚠️ CUDA: Project sudah tanpa PyTorch. CUDA via CTranslate2 langsung.
#    Untuk GPU, pastikan NVIDIA CUDA Toolkit terinstall di sistem.
Pillow>=10.0.0
tkinterdnd2>=0.3.0
fpdf2>=2.7.0
